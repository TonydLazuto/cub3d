#include "cub3d.h"
/*
void	draw(t_ptr ptr, int i, int color)
{
	int		x;
	int		y;
    t_data  img;

	x = 0;
	y = 0;
    img.img = mlx_new_image(ptr.mlx, WIDTH, HEIGHT);
    img.addr = mlx_get_data_addr(img.img, &img.bits_per_pixel, &img.line_length, &img.endian);
	draw_form();
    mlx_put_image_to_window(ptr.mlx, ptr.win, img.img, 10, 0);
}

void	draw_form(t_ptr ptr, int i, int color)
{
	while (y < 30)
	{
		while (x < 30)
		{
			my_mlx_pixel_put(&img, 5 + x, 5 + y, color);
			x++;
		}
		x = 0;
		y++;
	}
}

void	draw_wall(t_data *img, int color)
{
	int		x;
	int		y;

	x = 0;
	y = 0;
    img->addr = mlx_get_data_addr(img->img, &(img->bits_per_pixel), &(img->line_length), &(img->endian));
	while (y < 30)
	{
		while (x < 30)
		{
			my_mlx_pixel_put(img, 55 + x, 55 + y, color);
			x++;
		}
		x = 0;
		y++;
	}
}

void	draw_sprite(t_data *img, int color)
{
	int		x;
	int		y;

	x = 0;
	y = 0;
    img->addr = mlx_get_data_addr(img->img, &(img->bits_per_pixel), &(img->line_length), &(img->endian));
	while (y < 30)
	{
		while (x < 30)
		{
			my_mlx_pixel_put(img, 55 + x, 55 + y, color);
			x++;
		}
		x = 0;
		y++;
	}
}

void	draw_pers(t_data *img, int color)
{
	int		x;
	int		y;

	x = 0;
	y = 0;
    img->addr = mlx_get_data_addr(img->img, &(img->bits_per_pixel), &(img->line_length), &(img->endian));
	while (y < 30)
	{
		while (x < 30)
		{
			my_mlx_pixel_put(img, 55 + x, 55 + y, color);
			x++;
		}
		x = 0;
		y++;
	}
}*/