#include "cub3d.h"

int     check_resolution(unsigned int i, char *line, t_cub *cub)
{
    /*
    i++;
    i = skip_space(i, line);
    while (line[i] && ft_isdigit(num) == 1)
        i++;
    i = skip_space(i, line);
    while (line[i] && ft_isdigit(num) == 1)
        i++;
    i = skip_space(i, line);
    if (line[i] != '\0')
        return (-1);
        */
}

int     check_floor(unsigned int i, char *line, t_cub *cub)
{
    printf("floor\n");
    return(0);
}
int     check_ceiling(unsigned int i, char *line, t_cub *cub)
{
    printf("ceiling\n");
    return(0);
}
int     check_sprite(unsigned int i, char *line, t_cub *cub)
{
    printf("sprite\n");
    return(0);
}